var SessionStore = require('./lib/session_store.js')

var chai = require('chai')
var expect = chai.expect

describe('TODO', function() {

	it('should fail', function(done) {

		done(new Error('need to add unit tests'))

	})

})